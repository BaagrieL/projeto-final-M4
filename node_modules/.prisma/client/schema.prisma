generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model doadores {
  id_doadores Int     @id @default(autoincrement())
  nome        String?
  idade       Int?
  email       String?
  telefone    String?
}
